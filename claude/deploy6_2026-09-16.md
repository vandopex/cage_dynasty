# DEPLOY #6 — FOUNDING-ROW2 to PA (2026-09-16)

Status: DEPLOYED. Main was 2 commits ahead of PA `6d7abc2`; fast-forward
to `46c094fd1eeb9d94e2549c7ad2f9bdc6cb94f287`. All 10 gates green (§3).
Both owed browser checks closed one sitting (§4).

## 1. MECHANISM

Standing procedure per `claude/deploy5_2026-09-15.md` §1. No changes
this deploy: console warm-check at step 0, file-hash gate at step 6b
(Files API GET → shasum vs local), log-timestamp gate at step 6c
(respawn inside reload window).

## 2. PROOF TRIAD (verbatim)

**Local (step 1):**
`git ls-remote origin main` → `46c094fd1eeb9d94e2549c7ad2f9bdc6cb94f287 refs/heads/main`

**Pre-state (step 3, via console 47103182):**
```
04:28 ~/cage_dynasty (main)$ cd ~/cage_dynasty && git rev-parse HEAD && git status --porcelain && wc -l /var/log/vandopegaming.pythonanywhere.com.error.log
6d7abc2defb2f0ffd132e7617d06b82ebddb91ba
 M fight_engine.py
?? outputs/sm1/
148 /var/log/vandopegaming.pythonanywhere.com.error.log
```

**Pull (step 4):**
```
Updating 6d7abc2..46c094f
Fast-forward
 9 files changed, 829 insertions(+), 83 deletions(-)
```
Post-pull HEAD `46c094fd1eeb9d94e2549c7ad2f9bdc6cb94f287`. Drift
(` M fight_engine.py` + `?? outputs/sm1/`) preserved.

**Reload (step 5):** `POST /webapps/vandopegaming.pythonanywhere.com/reload/`
→ `{"status":"OK"}` HTTP 200. Wall-clock window `2026-09-16T04:29:05Z → 04:29:15Z`.

**File-hash gate (step 6b):**
```
PA sha256:    106e11af2e0cfed84d97bc933d0342cdb9ff041fffb71bc0d45f588e1bd55edc  /tmp/pa_gb_deploy6.py
local sha256: 106e11af2e0cfed84d97bc933d0342cdb9ff041fffb71bc0d45f588e1bd55edc  cage_dynasty_web/game_bridge.py
```
Byte-identical. PA game_bridge.py fetched via Files API against
`/home/vandopegaming/cage_dynasty/cage_dynasty_web/game_bridge.py`;
local at HEAD `46c094f`.

**Log timestamps (step 6c):**
```
server.log:
2026-09-16 04:29:15 ✅ world_init: FULL fight engine loaded — pre-gen will use simulation.fight_integration (shimmed to web fight_integration; fe retained as fallback)
2026-09-16 04:29:15 WSGI app 0 (mountpoint='') ready in 2 seconds on interpreter 0x77a7debf6ab0 pid: 1
2026-09-16 04:29:15 gracefully (RE)spawned uWSGI master process (pid: 1)
2026-09-16 04:29:15 spawned uWSGI worker 1 (pid: 2, cores: 1)
```
`(RE)spawned uWSGI master` at `04:29:15` inside the reload window
`04:29:05 → 04:29:15`. Zero 🚨 SIMULATION-SHIM FAILED lines in either
log. **Observation, not blocking:** one SIMULATION-SHIM print landed
in server.log this deploy (`2026-09-16 04:29:15 ✅ [SIMULATION-SHIM]
simulation.fight_integration shimmed …`) rather than error.log (where
prior deploys' shim prints appeared). Routing varied vs prior deploys;
substantive gate held (real engine loaded, no failures).

**Template compile sweep (step 7):**
```
=== TEMPLATE COMPILE SWEEP: 38/38 clean ===
```

## 3. GATES TABLE (10/10)

| step | expected | observed | PASS/FAIL |
|---|---|---|---|
| 0 | console 47103182 warm | HTTP 200; prompt `04:26 ~/cage_dynasty/cage_dynasty_web (main)$` | PASS |
| 1 | ls-remote 46c094f | `46c094fd1eeb9d94e2549c7ad2f9bdc6cb94f287` | PASS |
| 2 | 200, domain listed | 200; id 3095933; py 3.13; enabled | PASS |
| 3 | HEAD 6d7abc2, known drift, error.log 148 | 6d7abc2; drift matches; 148 lines | PASS |
| 4 | Fast-forward 6d7abc2..46c094f | 9 files, +829 −83; drift preserved | PASS |
| 5 | reload 200 | 200; 04:29:05Z → 04:29:15Z UTC | PASS |
| 6a | .git ref = 46c094f | `46c094fd1eeb9d94e2549c7ad2f9bdc6cb94f287` | PASS |
| 6b | file hash byte-identical | PA `106e11af…5edc` = local | PASS |
| 6c | respawn + world_init loaded at reload time | (RE)spawned at 04:29:15; FULL fight engine at 04:29:15; 0 FAILED | PASS |
| 7 | 38/38 template compile clean | `=== TEMPLATE COMPILE SWEEP: 38/38 clean ===` | PASS |

**All 10 checks PASS.**

## 4. BROWSER CHECK — BOTH OWED ITEMS CLOSED

Van's sitting on the freshly-deployed PA save, 2026-09-16.

### FOUNDING-ROW2 — verified player-visible

- **Piotr Jedrzejczyk**, founding Welterweight champion, record **9-0-0**, streak reads **9**. Pre-fix would have read 10 (founding row leaking into the tail count).
- All five rows on both streak boards (active + longest) satisfy `streak <= wins`.
- **Tsarukyan** (10-1-0, loss mid-run) correctly shows active=4 — post-loss reset; no leakage.

Docket closed on player-visible surface. Retirement-news reader
(`game_bridge.py:11416`) also switched to canonical this ship — surfaces
correctly on any future retirement without stored-value dependence.

### REMATCH-COOLDOWN1 — verified

- **Tsarukyan def. Nicholas Bailey at Cage Dynasty 62** (live-play,
  week 2) having beaten him at **Cage Dynasty 49** (pre-gen).
- Structurally impossible pre-`9faeba8` — the old `current_week -
  fought_week` formula would have returned a large negative on the
  pre-gen row and every cooldown gate (thresholds 20/20/16/6) would
  have blocked the pair as `last < N`.

Docket closed. Owed since before this session.

## 5. FILED (new dockets + amendments)

### NEW — DRAW-SEMANTICS1 upgraded, direction corrected

The prior filing (from FOUNDING-ROW2's lose_streak gate, 2026-09-15)
recorded `result='L'` for **both** fighters on a draw. Van's browser
session finds the **opposite direction** on the CANONICAL live surface:

- **Emeka Usman** fight history row: `result=WIN, method=DRAW`, Cage
  Dynasty 46 vs Charles Baker.
- Record 4-3-0 (zero draws in the record header).
- Finish breakdown reports "By DEC 3" against **two** actual decision
  wins — the draw is counted under DEC WINS.

So a draw likely **splits** into W for one side and L for the other,
not L for both. The earlier dump sample (Emil Lindqvist row with
`result='L'`) was the loser side; Usman's row was the winner side. **No
fighter on any player-visible surface in a fresh save carries a draw in
their record; the D column appears permanently zero.** Engine-tier,
write-path, forward-only. Not scoped.

### NEW — BELT-LINEAGE1

Champions page, Lightweight division. Machado won the belt from
**Steven Thompson at CD59** (per his own profile), but the Champions-
page lineage reads **"0 Title Changes / 1 Total Reign"** and labels
Machado **"Inaugural Champion at Season Opener."** Thompson's reign is
invisible. Same two-store shape suspected as RECORD-BOOK1 (record book
blank sections) and VACATED-REIGN1 (belt_history vs _title_history
diverge). **File all three as one arc candidate.**

### NEW — RECAP-STALE1

Week 4's weekend recap re-displayed Week 2's card verbatim — **CD62,
Tashkent, all eight fights, same results.** CD63 never appeared; CD64
showed at week 6. Finance ledger confirms weeks advanced. Isolated to
the recap render, not the underlying week counter.

### NEW — HISTORY-CAP1

Fight history appears **capped at 10 rows on the profile**. Tsarukyan
profile states "11 fights" (record header) but lists only 10 rows in
the history table. His first reign claims **6 defenses** against **5**
visible wins in that window. Display-tier cap suspected.

### NEW — NAME-COLLISION1

**"Steven Thompson"** and **"Stephen Thompson"** are distinct fighters,
both in Machado's history four events apart. Two readings: intentional
(real MMA has repeat surnames) or generator near-miss (should dedup on
edit distance). Confirm which is intended.

### NEW — DIVISION-GENDER1

**Karolina Grabowski** ranked **#1 Welterweight**, contested the title
vs Piotr Jedrzejczyk. Mixed-gender division. Confirm whether intended.

### NEW — HARNESS-ENV1

Under `save_invariants`'s `_harness_env`, bare `import fight_engine`
resolves to `systems/fight_engine.py` (CLI dead copy) instead of
`cage_dynasty_web/fight_engine.py` (web copy). Consequence today:
`FULL_ENGINE_AVAILABLE=False` locally when running save_invariants; PA's
wsgi.py setup puts `cage_dynasty_web/` at sys.path[0] and gets
`FULL_ENGINE_AVAILABLE=True`. The `sys.path.insert(0, ...)` loop in
`claude/tools/_harness_env.py` is not idempotent against pre-existing
script-dir / PYTHONPATH entries. Historic dumps in `outputs/sm_inv/`
(measured 2026-09-15 by the DRAW/method-distribution test) were
generated when the environment resolved bare imports to the web copy —
likely a different invocation shell at generation time (PYTHONPATH,
CWD, or `-m` module invocation). Not fixed. Existing dumps are real-
engine; new dumps produced by today's shell are fallback until this
docket is resolved.

### NEW — DUMP-PROVENANCE1

`save_invariants` dump format records `seed, weeks, mode` but not
`generating commit`, `timestamp`, `command line`, or
`FULL_ENGINE_AVAILABLE` state at generation. Measured 2026-09-15 on
`outputs/sm_inv/dump_20260908.json` (top-level keys: `['belt_history',
'fighter_data', 'fighters', 'gen_fighters', 'meta', 'mode',
'player_camp_id', 'seed', 'weeks']`; `meta = {'week': 14}`). The
DRAW/method-distribution test (12 DRAW rows + 0.5% KO rate vs
fallback's 18% KO and 0 DRAWs) is the interim retroactive classifier:
fallback worlds have 0 DRAWs and ~18% KO; real-engine worlds have DRAWs
and KO ≪ 18%. Sweep of all 25 dumps on disk (2026-09-15) confirmed 21
save_invariants dumps in `outputs/sm_inv/` and 4 `/tmp` session dumps
are real-engine; only two `outputs/sm1/saveload1/scratch_saves/bridge_van_sl1t2_r*.json`
are fallback (predate `_harness_env`). Not fixed.

### CLOSED — FOUNDING-ROW2

Shipped `46c094f` (2026-09-15 commit / 2026-09-16 PA deploy).
Player-visible verification (§4). Retirement news + record book both
switched to canonical filtered pair; no other player-visible reader
consumes stored `best_win_streak`.

### CLOSED — REMATCH-COOLDOWN1

Shipped `9faeba8` (2026-09-14 commit / deploy #5 2026-09-15 PA).
Player-visible verification (§4) at deploy #6 sitting. Docket owed
since before the current session; closed.

### CORROBORATION — COOLDOWN-CONSTANTS1 / TITLE-CADENCE1

Piotr Jedrzejczyk's inaugural Welterweight reign shows **9 defenses
across 9 pre-gen wins** — every fight of his career was a title fight.
Week 6's live card was **#13 vs unranked** main event with **six
unranked prelims**, two weeks after two title fights. Consistent with
the champion-metronome pattern COOLDOWN-CONSTANTS1 flagged and the
"title cadence bunches then droughts" pattern TITLE-CADENCE1 captured.
Not new; corroborates existing dockets.

## What next

Both major browser-owed items closed. The eight new dockets (one
upgrade + seven new) sit on the board for scoping. Two of them
(BELT-LINEAGE1 + RECAP-STALE1) are player-visible template/display
issues. HARNESS-ENV1 and DUMP-PROVENANCE1 are instrument-side and
non-blocking. DRAW-SEMANTICS1 remains engine-tier, write-path — likely
the largest new arc surfaced this session.
