# DEPLOY #3 — PLAYER-CREATE1 TO PA (2026-09-08)

Status: ACCEPTED ON PROOF. PA HEAD 7555c3504e0b68c15f3a6da15960ab8c37493ce8
(from 23de048, deploy #2). Payload: cf2fecf (docs), f0bf2f3 (tools +
Gate 0 filing), 4f50b78 (a), 1839545 (b), 64a879f (c), 7555c35 (docs).
14 files, +2632/−20. Filing for the code: claude/player_create1_filing_2026-09-08.md.

## 1. MECHANISM (G2 — verbatim)

Push (Van, local shell): first attempt from ~ failed ("'origin' does not
appear to be a git repository" — wrong cwd, not a remote problem); from
the repo: `git ls-remote origin main` showed f0bf2f3 with local
`[ahead 4]` — the push had NOT run yet. Then
`git push origin main` → `f0bf2f3..7555c35 main -> main`; ls-remote
7555c35; `## main...origin/main` no ahead/behind.

PA console (Van, browser console, ~/cage_dynasty):
  pre-state: `git rev-parse HEAD` 23de048; `git status --porcelain`
    = ` M fight_engine.py` (filed root-copy drift) + `?? outputs/sm1/`
    (PA-SMOKE1 residue). Both expected.
  error-log baseline: `wc -l` 137 → copied to ~/errlog_before_d3.txt.
  pull: `git fetch origin && git pull --ff-only origin main && git
    rev-parse HEAD && git status --porcelain` → Fast-forward
    23de048..7555c35; HEAD 7555c35; status unchanged (same two lines).

Reload: API call
  `curl -X POST -H "Authorization: Token $PA_TOKEN" .../webapps/vandopegaming.pythonanywhere.com/reload/`
  returned 401 from BOTH Van's local shell and the PA console —
  $PA_TOKEN is unset in both (the deploy.sh:5 literal is the revoked
  token from the prior leak). Fallback used: the Reload button on the
  PA Web tab, at 04:53 PA time. Recorded here per G2; the API path
  needs TOKEN-HYGIENE1 (below) before it works again.

## 2. PROOF TRIAD (PA console, read-only)

3a. `cat .git/HEAD && cat .git/refs/heads/main` →
    `ref: refs/heads/main` / 7555c3504e0b68c15f3a6da15960ab8c37493ce8.
    Independent of the pull line's inline read.
3b. Markers on the RUNNING import path (web copy, not systems/):
    cage_dynasty_web/game_start.py:552
      `derived = FighterGenerator._phase_b_derive_style(`
      (also :455, :560 — docstring and comment)
    cage_dynasty_web/game_bridge.py:2668
      `_fdata['ovr_at_signing'] = int(fighter.overall_rating)`
    cage_dynasty_web/templates/training.html:463
      `('recovery','Recovery'), ('power','Power'),`
    cage_dynasty_web/templates/compare.html:60
      `('Composure','composure',None),`
    Four of four. Not a phantom.
3c. Error log 137 → 140 lines. New lines, all 04:53:04 (after the
    04:50 pull):
      04:53:04,223 ✅ [SIMULATION-SHIM] simulation.fight_engine shimmed …
      04:53:04,224 ✅ [SIMULATION-SHIM] simulation.fight_integration shimmed …
      04:53:04,252 OSError: write error
    The first two are deploy #2's benign pair. The third is NEW vs
    deploy #2 and is EXPLAINED by the server log (same second):
      gracefully (RE)spawned uWSGI master (pid 1); spawned worker 1;
      ✅ world_init: FULL fight engine loaded …;
      SIGPIPE: writing to a closed pipe … on request / (ip 10.0.4.129);
      uwsgi_response_writev_headers_and_body_do(): Broken pipe … during GET / (10.0.4.129);
      announcing my loyalty to the Emperor...
    An internal (10.0.4.x) GET / during the respawn disconnected before
    the response finished; the broken pipe surfaced as the OSError.
    Worker came up healthy. After Van's own page loads the error log
    was still 140 lines — no errors from real requests. Documented as
    explained, not open.

## 3. STANDING GATES

G0 (PA console): `python3 -u claude/tools/template_compile_sweep.py | tail -1`
   → `=== TEMPLATE COMPILE SWEEP: 38/38 clean ===` on the 7555c35 checkout.
G1: no harness run on PA this deploy (CPU quota respected; PA-SMOKE1
   is its own item).
G2: §1 above.

## 4. BROWSER CHECK (fresh save, week 0 → 1)

- /training: 19 tiles; Power sixth in the physical group after
  Recovery (value 69). Header still reads "63 OVR → 0.0" (filed nit).
- Profile at week 0: Overall 63, At Signing 63, Growth "—", Ceiling
  74; style badge "Balanced" (PROSPECT-BONUS1, expected); Power 69 in
  the physical block; height/reach still `5'10" / 72""` (HEIGHT-REACH1
  + the double-quote render, filed).
- /compare: 19 stat rows + Overall, Power between Recovery and Boxing.
  The god-stat pair (a 95-in-nine-stats Strawweight champion vs a
  Heavyweight) is playtest #6's census item, pre-existing.
- Advance one week: Training Report shows Power receiving the coach
  gain (+0.1) — the tile is wired to the weekly path. Profile at week
  1: Overall 63 → 63; Boxing 56→57, TD Defense 69→70, Fight IQ 62→63;
  At Signing 63 unchanged. The −4 first-advance drop (playtest #3) is
  gone.
- Not observed: the /setup-fighter card OVR for the chosen prospect
  (SETUP-OVR1's card-vs-profile gap). Stays filed; no number tonight.
- Seen for WEEK-AXIS1's Gate 0: the first live card on a fresh save
  is "Cage Dynasty 61" — event_number continues past the 60 pre-gen
  events. That is the axis fight rows get stamped on (ruling 4).

## 5. FILED FROM THIS DEPLOY

TOKEN-HYGIENE1 (sequenced with SECRET_KEY). deploy.sh carries two
  hardcoded literals: PA_TOKEN at :5 (the revoked token — dead) and
  DEPLOY_TOKEN at :8 for the git-pull webhook (status unknown; treat
  as live). If deploy.sh is tracked, both are in repo history. Fix:
  rotate DEPLOY_TOKEN; both read from files outside git
  (`PA_TOKEN=$(cat ~/.pa_token)`, chmod 600); never echo either in a
  command line (claude_md_archive_2026a.md:246-251). The architect
  paste `grep -n -i "token" deploy.sh` this session echoed the dead
  literal into the cc transcript — the paste was wrong, cc
  self-reported. No live credential exposed.

## 6. PROCESS NOTES

- The push was assumed done and wasn't; `git ls-remote` before the
  PA pull caught it. Without that line the pull would have
  fast-forwarded PA to f0bf2f3 (tools+docs only) and read as a
  "deploy #3" with none of (a)/(b)/(c) — a phantom by omission.
  Standing rule: ls-remote is the first line of every deploy paste.
- The reload button is a legitimate mechanism when recorded; the
  proof that it worked is 3c's timestamps + the server log respawn
  sequence, not the click.
- cc has no PA access. Every PA-touching command is Van's keystroke;
  cc drafts, Van runs, outputs are pasted to both.
