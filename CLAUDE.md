## Shipped 2026-05-01
- Bug AB (verified): per-fight FOTN badge spillover on event recap surfaces. Old buggy FOTN writer (pre-commit 56bf807) wrote `is_fotn=True` on every fight without clearing losers — slot3 events 1-30 had the corruption persisted on disk; events 31+ clean post-fix. Three template surfaces (`event_detail.html:80`, `week_results.html:153`, `archives.html:59`) read `fight.is_fotn` directly and rendered every old-event fight as FOTN. Fix: switched all three to derive from `event.fotn.fight_id == fight.fight_id` (event-level source of truth, populated by 2026-04-30's FOTN top-banner fix and `None` for legacy events). Self-fixes corrupted historical data without save migration — `event.fotn` is None for old events, so the conditional evaluates False everywhere → no badges rendered. New events match exactly one fight. Three template-only edits, identical pattern. Commit `69ff422`. Verified across all three surfaces × old/new events. Same "parallel sink / source of truth" architectural family as Bug Z and Bug AA.
- Diagnostics throughout the Bug AB hunt: two TEMPORARY-marked print blocks (FOTN block per-week + load-time audit at `web_load`) added to game_bridge.py during diagnosis, never committed (uncommitted instrumentation by design). Removed after verification — working tree returned to HEAD state, no removal commit needed.
- Bug AB.1 filed (NEW, NOT FIXED): `watch_fight.html:43` has the same FOTN spillover vulnerability shape but requires a ROUTE-level change because `routes.py:2010-2067` only passes `fight` to the template, not the containing `event`. Lower priority — single-fight context means spillover isn't visible the same way (no per-row comparison). Fix shape (route + template, ~10 lines) sketched in memo. See `memory/bug_AB1_watch_fight_fotn_spillover_2026-05-01.md`.
- Champion injury Slice 0.5 (verified): scheduled fights bypassed the injury filter that already guards new card construction at `game_bridge.py:7299` — champion auto-booked for a defense, gets injured between scheduling and event firing, fight ran with injured champion. Same hole on AI-vs-AI title defenses. Fix: three-site bundle adding `is_cleared_to_fight` checks at `advance_week:1259` (player loop), `_simulate_card_fights:5979` (AI card loop), `_simulate_ai_fights_week:6228` (AI fallback pool filter). Two patterns — loop-level `continue` skip on player+AI loops, filter-level exclusion on the fallback pool (mirrors existing card-builder filter at line 7299). Commit `e1b14fe`. Verified end-to-end on slot3: Lucas Adesanya (LGT champ) injured wk 35-36 → no defense booked, contenders auto-paired against each other; toe sprain cleared wk 37 → wk 38 ladder shows defense vs Freddie Johnson II at DFC 44 with no player intervention. Gabriel Costa fight-induced injury also gated correctly. All three guards short-circuit cleanly when injury system unavailable.
- Champion injury Slice 0.75 (verified): bumped pre-existing auto-vacate threshold at `game_bridge.py:1448` from 8 weeks to 25 weeks. Old threshold conflicted with design spec — Severe injuries (9-24w) auto-stripped belts that should hold pending player choice (Slice 3 work). New threshold cleanly demarcates Severe (≤24w, hold) from Career-level (≥25w, auto-vacate per spec). Two-line change (comment + value), no logic added. Commit `c3dfd47`. Filed deferred work: counter rename tech debt (`_champ_weeks_since_defense` actually tracks "weeks consecutively injured"; bundle when auto-vacate block is next touched) at `memory/tech_debt_champ_weeks_counter_naming_drift_2026-05-01.md`; Slice 5 sub-task to update existing vacate headline text ("interim title fight to be announced" references the unbuilt interim system) — amended into design spec at `memory/design_champion_injury_handling_2026-04-30.md`.
- Champion injury Slice 1 (shipped): champion-aware injury news at three sites (training `:3462`, fight score-fallback `:6120`, fight real-engine `:8157`) plus Slice 0.5 cancellation news at two sites (player loop `:1260`, AI card loop `:6001`). Champion injuries fire `🏆 [Name] ([Division] Champion) suffers [injury] — out [N] weeks. Title defense delayed.` Site A widens its prior player-only gate to (player OR champion), so AI champion training injuries now generate news for the first time; player non-champion path remains byte-identical 🤕. Sites B+C are pure conditional swaps (champion → 🏆, else → existing 🤕 unchanged). Cancelled fights from Slice 0.5's gate fire `🚫 [F1] vs [F2] at [Event] cancelled — [Uncleared(s)] not cleared to fight.` (handles single + both-uncleared via `" and ".join`). Site F (AI fallback pool filter) deliberately silent — filter-level exclusion has no specific scheduled fight to announce. Commit `804938b`. Verified by 3-week advance — existing 🤕 path firing through terminal output unchanged, no regressions on non-champion injuries. 🏆 and 🚫 surface naturally on champion-injury and gated-fight scenarios in future play.

## Shipped 2026-05-03
- Amateur tab Slice A (verified): six-component bundle + unified amateur fighter ID. Functional + data layer for the amateur tab; visual polish deferred to a future Slice C. Components: (1) tournament wins display on prospect rows with singular/plural handling — backend already supplied `tournament_wins`, template now renders inline. (2) Missing weight-class divisions surfaced — `get_amateur_overview` was hardcoding 6 of 9 divisions; replaced with `from amateur import WEIGHT_CLASSES` so Strawweight/Flyweight/Light Heavyweight prospects now appear. (3α) Unified amateur fighter ID at sign — `sign_amateur` was minting `fid = f"amateur_{amateur_id[:12]}"` (two IDs for one logical fighter); audit confirmed the prefix had zero sentinel readers and ID spaces are disjoint by format, replaced with `fid = amateur_id` so identity travels intact from amateur registry into pro registry. (4β) Bridge fallback to amateur registry in `get_fighter` — pro lookup primary; on miss, falls through to `amateur_system.amateurs.get(...)` and stub-converts via new `_convert_amateur_fighter` helper. Unsigned amateurs lookupable via `/fighter/<id>` and render through the polished pro fighter_profile template (degraded but coherent — no camp, no pro ranking, no fight history; full identity + attributes + style banner + stat blocks). (5γ) Sherdog-style amateur credential on profile — sign flow no longer copies amateur W/L into pro `FighterRecord` (pro starts 0-0 per real-world MMA convention); route passes `amateur_record` context if `amateur_system` has an entry under this fighter's ID; template renders below the existing By KO/SUB/DEC subtitle as `AMATEUR: 12-0 · 🏆 3 tournament wins` with a small gold "Amateur" label. (6) Three fighter-name link sites in `amateur.html` — prospect names, tournament champion names, regional ranking names now wrap in `<a href="/fighter/<id>">`; bridge `regional_rankings` dict gains `fighter_id` field for the third link site. Post-verification fix: `_convert_amateur_fighter` hardcodes `wins=0/losses=0` so unsigned amateur profiles show 0-0-0 pro record (not amateur W/L) — consistent with sign-time behavior; lifecycle now: unsigned `0-0-0 + AMATEUR: 4-7`, post-sign `0-0-0 + AMATEUR: 4-7`, after first win `1-0-0 + AMATEUR: 4-7`. Net diff: +141 / -20 across 4 files (`game_bridge.py +94`, `routes.py +17`, `amateur.html +14`, `fighter_profile.html +13`). Commit `af14904`. **Architectural change:** amateur fighter identity now unified across amateur-to-pro transition. **Behavior change:** future signed amateurs start at 0-0 pro record instead of carrying amateur W/L; existing slot3 signed amateurs keep legacy stats (no backfill per willing-to-overwrite-saves stance). Verified across all 9 divisions surfacing, tournament wins displaying, three link sites functional, Sung Yoon profile (0-0-0 pro + AMATEUR: 7-4), Erik Lopez profile (0-0-0 pro + AMATEUR: 4-7). **Ship #14 — Sunday's second ship.** Closes/affects amateur tab audit memo (partial — functional + data layer shipped, visual polish Slice C remains).
- AI signing pipeline fix (verified, **compounds-with-playtime class**): `_ai_bid_for_free_agent` at `game_bridge.py:9310-9353` was setting `fighter.camp_id` and discarding from `free_agents` but skipping 5 of 7 mutations from the canonical `_sign_fighter_to_camp` helper: contract creation (`fighter.contract_id`), camp roster cap counter (`camp.fighter_count`), camp roster list (`_camp_data[camp_id]["fighters"]`), `active_contracts` entry, `_contract_data` entry. Visible behavior was correct — signed fighters fought via card-builder's `camp_id != player_camp_id` check (the Superbon Yusupov "signed + fought DFC 41 same week" anecdote held up empirically). But underlying state drifted: AI camps could over-sign past their `max_fighters` cap because the counter never advanced, AI-signed fighters had no contracts to expire, UI camp-detail roster lists silently missed them. Fix: replace the two-line manual mutation at `9342-9343` with a single call to `self._game_state._sign_fighter_to_camp(fighter_id, winning_camp.camp_id)` + defensive `if not contract_id: return` to suppress news on failed signs. +5/-2 lines net. Commit pending. Verified: save/load round-trip clean, signings continue to fire `⚔️ [AI SIGNING]` log, camp roster lists now include AI-signed fighters, contract expiry will surface naturally over weeks of playtime. **Closes** `memory/todo_ai_signing_pipeline_verification_2026-05-02.md` (outcome (b)). **Unblocks** `memory/todo_signing_and_debut_alerts_2026-05-02.md` — alerts feature can ship as future pickup; pipeline now sound. **Thirteenth ship of the arc.**
- Signing + debut alerts (verified, **Ship #15 — Sunday's third ship**): two-site bundle adding fighter-flavored news texture for AI signings and pro debuts. Builds on Ship #13 (pipeline correctness — fighter_count now tracks accurately, so the OVR filter is meaningful) and Ship #14 (unified amateur ID + amateur registry navigability — debut detection can route through `amateur_system.amateurs.get(fid)` because IDs travel intact from amateur into pro). Three-ship coherent unit signaling signings/debuts/identity to the player. Goal #1 — enhanced AI signing news at `_ai_bid_for_free_agent`: replaced camp-flavored headline (`{emoji} {fighter} signs with {camp} — {arch}`) with fighter-flavored format (`{emoji} {fighter} signs with {camp} — {style}, OVR {X}`) — style and OVR are the player-facing identity bits, archetype was a backend label. Filter gate added: `if _ovr >= 70 or _has_fights:` — suppresses world-init churn (low-OVR rookies with no fight history don't generate news) while letting both established prospects (Dennis Thomas OVR 62 + has fights) and high-OVR signings (any 70+ without fights) through. Terminal `[AI SIGNING]` log unchanged for debug visibility. Goal #2 — new pro debut alert at `_apply_post_fight_camp_record` (Bug F's hoist): single-site insertion in the existing winner+loser for-loop, sibling pattern to earned-nickname feature inserted just above. Detection: `len(fight_history) == 1 AND amateur_system.amateurs.get(fid)` truthy — first pro fight AND amateur-registry-tracked (excludes generated free agents whose first fight isn't a "debut"). Symmetric headlines with opponent named in both branches: `🆕 {fighter} wins pro debut over {opponent} at {event} — {method}` / `🆕 {fighter} drops pro debut to {opponent} at {event} — {method}`. Net diff +24 lines, `game_bridge.py` only. Commit `e0fba25`. Verified Goal #1 clean on slot3 — Dennis Thomas (OVR 62, has-fights) and Chase Garcia (OVR 49, has-fights) both surfaced with new format; OR-gate caught both via fight history. Goal #2 ships on code-reading (gate logic mechanical, same compound-rare-trigger discipline as Slices 2/2.5/3 — surfaces naturally when signed amateur debuts). **Closes** `memory/todo_signing_and_debut_alerts_2026-05-02.md`. **Filed alongside:** `memory/todo_news_contracts_tab_2026-05-03.md` (news category bar lacks tab for existing "contract" category — recommend Option A 📋 Free Agents tab; ~3-5 lines template-only; not blocking; future pickup).
- Amateur tab Slice C Part A (verified, **Ship #18 — Sunday's sixth ship · amateur tab visual polish**): five-site bundle bringing the amateur tab UI quality up to Saturday's fighter profile polish bar (commit `f9c2a07`). Pure list-view enhancement — no card grid, no row height growth, density preserved per the audit's "keep list" verdict. **P1** family-colored prospect rows: 3px left-border in striker-red / grappler-blue / hybrid-violet for instant style triage; style icon + family-colored style name + tagline replace the plain style text in the metadata line. **P2** tier-colored OVR in display font: gold (85+) / neon-green (70+) / info (55+) / muted (<55) thresholds matching `fighter_profile.html`; display-font for broadcast-graphic feel. **P3** removed duplicate grade display: lines 103-109 of the prior file repeated the inline grade rendered next to the name; freed vertical space without information loss. **P4** tournament credentials display: bridge addition exposing `tournament_finals` + `tournament_semis` from `AmateurFighter` dataclass (2-line dict pass-through at `game_bridge.py:9826`, same shape as Slice A's `regional_rankings.fighter_id`); template renders all three credential tiers (wins/finals/semis) conditionally with singular/plural agreement. Format reads like Sherdog: "🏆 2 wins · ⚔️ 1 final · 🥉 4 semis" — Naoya Inoue with "🥉 4 semis" is meaningfully different from a prospect with zero credentials. **P11** empty-state placeholders: Recent Tournaments and Regional Rankings sections previously vanished entirely when empty; now render placeholder cards with explanatory copy ("No tournaments yet — they fire periodically. Advance a few weeks." / "Rankings populating as fighters compete..."). **Architectural decision — extracted style maps to a new shared macro file** instead of duplicating fighter_profile.html's inline maps. Reasoning shifted mid-session: audit underestimated map size (~30 lines for three maps not 15), Slice C Part B is queued and may want style filtering (third consumer within a week), macro overhead is small (~3 lines import per consumer). New file `templates/_macros/style_data.html` (41 lines) is the single source of truth for fighting-style → family → tagline → icon. `amateur.html` imports via `{% from '_macros/style_data.html' import style_icons, style_class_data, style_family_color with context %}`. `fighter_profile.html` stays with inline maps for now — refactor pending under tech debt note for next-natural-touch (`memory/tech_debt_fighter_profile_style_maps_extract_2026-05-03.md`). **Recovery during apply:** regional-rankings empty-state insertion broke template structure on first attempt (Jinja `{% if/else %}` mid-block injection between opening `{% if %}` and the for-loop body); caught via grep of structure, fixed via single replacement edit of the whole regional rankings block. Discipline pattern: when editing Jinja control structures with nested for-loops, grep the file post-edit to verify before assuming the next edit anchors are intact. Net diff: +73 / -9 across 3 files (1 new) — `_macros/style_data.html` 41 new lines, `amateur.html` +30/-9 = +21 net, `game_bridge.py` +2. Commit `3695980`. Verified clean on slot3: family-colored borders rendering correctly (red/blue/violet), tier-colored OVR code-read verified (no high-tier amateurs in slot3 to surface gold/green at runtime), dupe grade gone, tournament credentials displaying (Naoya Inoue example), Jinja templates parse clean. P11 empty states code-read verified (slot3 has tournaments + rankings populated; live verification needs a fresh world). **Closes (Part A)** `memory/todo_amateur_tab_ui_audit_enhance_2026-05-02.md`. **Filed alongside:** `memory/tech_debt_fighter_profile_style_maps_extract_2026-05-03.md` (fighter_profile inline-maps refactor pending) and `memory/slice_c_part_b_filter_sort_trajectory_2026-05-03.md` (queues Tier 2 polish: filter chips by weight class + maybe grade-band + maybe potential-ceiling-band, sort dropdown by Rating/Potential/Tournament Wins/Grade, trajectory mini-bar using dormant `potential_ceiling`, eligibility reason label using dormant `eligibility_reason`; ~80-100 lines, six design questions to lock in Phase 1 before code).
- News feed Free Agents tab (verified, **Ship #17 — Sunday's fifth ship · surgical UX gap close**): added `('contract', '📋 Free Agents','btn-primary','contract')` tuple to the cats list at `templates/news.html:17`, positioned between 📝 Signings and 💪 Training to reflect the conceptual flow (contract expires → free agent → AI signs them). Backend already supported the filter — `routes.py:1067` does generic string-equality on `n.category`, and 5 writer sites in `game_bridge.py:6713/9469/9485/9493/9505` emit `category=contract` items. Only the UI tab was missing — items appeared under "All" but couldn't be filtered to. Single +1 line template-only change. Commit `fd41007`. Verified on slot3 — new tab visible between Signings and Training, clicking it filters to contract items only ("📋 Chase Garcia is a free agent — contract with Syndicate Fitness expired" and similar), other tabs unaffected. **Closes** `memory/todo_news_contracts_tab_2026-05-03.md` (filed → shipped same day; surfaced during Ship #15 verification). **Filed alongside:** `memory/tech_debt_news_categories_list_inconsistency_2026-05-03.md` (routes.py:1069 has dead `categories` list passed to template that hardcodes its own cats tuple; lists also disagree — route has ranking/retirement, template has streak/record/training/scouting/facility; DRY hygiene not functional bug; recommend Option A delete-dead-list on next news routing/template touch).
- Champion injury Slice 4 (verified, **Ship #16 — Sunday's fourth ship · completes the foundational champion injury arc**): three-component bundle closing the hold-path consequences. The full arc from Slice 0.5 (pre-fight injury gate) through Slice 4 (hold-path consequences) is now scaffolded — Slices 0.5/0.75/1/2/2.5/3/4 shipped. Component 1 — cardio decay during recovery: tapered weekly rate (weeks 1-4 = -0.25/wk, 5-12 = -0.5/wk, 13+ = -0.75/wk) with float accumulator. Cardio is `int`; fractional rates need accumulation across weeks to be visible — `cardio_decay_accum` field on hold entry tracks fractional accumulation, `while _accum >= 1.0 and _ftr.cardio > 1: _ftr.cardio -= 1; _accum -= 1.0` applies integer decrement when threshold crossed. New per-week loop iterating `_champion_holds.items()` inserted between aging and maintenance training (Component 1's natural sibling to the existing maintenance idle-decay pattern). Decay sticks until trained back — no auto-rehab boost. Cardio only for v1; strength/speed unaffected. Component 2 — mandatory return defense booking on heal: new `_book_mandatory_return_defense(champion_id, wc, exclude_ids)` helper (~38 lines, mirrors `_book_vacant_title_fight` structure but with champion fixed, opponent = top1 from preview after excluding champion). Indefinite retry on booking failure — entry stays alive in `_champion_holds` until booking succeeds. Three-branch heal-sweep logic: (a) champion already booked into a fight → existing fight is the return, pop entry, generic recovery still fires; (b) booking succeeds → fire replacement headline, pop entry, suppress generic; (c) booking fails → keep entry alive, retry next week, generic recovery fires. The mandatory-defense fight is auto-booked WITHOUT player offer/prompt — this is the locked-in cost of having chosen HOLD over VACATE in Slice 3. Player agency was exercised at the decision point; mandatory defense is the consequence. **First auto-booked player title fight in the codebase** — Slice 2 explicitly excludes player fighters, Slice 2.5 issues an offer instead of booking, Slice 4 introduces the new pattern. Component 3 — news tone progression: milestone hype headlines at 4w/2w/1w until return (`🏆 {Champion} {N} weeks from return — {WC} title defense pending` family) fired from Component 1's per-week scan loop using `==` gate (assumes monotonic countdown — acceptable for v1; would need `_fired_milestones` set if future features mutate `return_week` mid-hold). On heal/booking SUCCESS: replacement combined headline `🏆 {Champion} returns to action — {WC} title defense booked vs {Contender} at {Event}` fires from inside the helper for symmetry with `_book_vacant_title_fight`. Generic recovery line at `:1454-1463` gated on `fid_h not in _slice4_handled` — champion-with-successful-booking skips generic, gets the dedicated headline instead. On heal/booking FAILURE: no news, retry silently (terminal warning still fires from inside helper). Other (non-champion) recovery news unchanged — generic pattern preserved for everyone else. Architectural changes: heal-sweep refactored from Slice 3's destructive inline-pop (lines 1631-1638) to two-phase pop+process+delete pattern; moved to line 1453 to run BEFORE the generic recovery loop so the handled-set can gate that loop (capture metadata, decide branch, conditionally pop). New `_champion_holds["cardio_decay_accum"]` field round-trips via existing `to_dict`/`from_dict` at `:1072+1146`; backward-compat default of 0.0 via `.get("cardio_decay_accum", 0.0)` handles existing entries — no save migration. Net diff +131/-9 = +122 lines, `game_bridge.py` only. Commit `7700775`. Verified clean on slot3 — no current player champion holding, so no Slice 4 trigger; advanced several weeks confirming no regressions on existing flows; AI champions Bruno Carvalho and Francisco Ortiz took Minor injuries that correctly stay outside Slice 3/4 territory; recovery news fires normally for non-champion fighters; no orphan `cardio_decay_accum` errors. Compound-rare-trigger reality: Slice 4 surfaces naturally when player gets a champion who takes a Severe injury and chooses HOLD — same discipline as Slices 2/2.5/3. **Filed alongside:** `memory/tech_debt_preview_vacant_contenders_min_count_2026-05-03.md` (`_preview_vacant_title_contenders` requires ≥2 contenders to return non-None; Slice 4 only needs top1 but inherits the constraint, silently deferring 1-contender divisions; refactor with `min_contenders` param when a third caller emerges or scenarios bite in practice; low priority, degenerate edge case). **Slice 5 (interim belts) and Slice 6 (ring rust polish) remain queued — the foundational arc is now complete.**
- /prospects route retired (verified, **Ship #19 — Sunday's seventh ship · sidebar redundancy cleanup, largest negative-scope ship of the four-day arc**): five-site bundle removing the misleadingly-named `/prospects` page after Ships #14 (amateur ID unification) and #18 (visual polish) made `/amateur-circuit` a legitimate scouting destination. Pre-retire, `/prospects` rendered three sections: (1) player's own developing roster filtered on `OVR < 75 AND age < 27` (brittle filter — using OVR as a development gate is suspect now that OVR is player-facing-only per the 2026-04-27 design principle), (2) watchlisted fighters preview (duplicating `/watchlist`), (3) "Find New Talent" CTA card pointing to `/amateur-circuit`. Three structural problems: misleading naming (player clicking 🌱 Prospects expecting raw amateur talent landed on their own internal roster), content overlap with `/watchlist`, stale OVR-based filter. Three options considered: (A) rename to clarify intent, (B) merge into richer "Scouting Hub" landing page, (C) retire entirely. **Picked (C)** — page mostly existed as CTA gateway + watchlist echo; player capabilities preserved via `/roster` (developing fighters), `/watchlist` (watchlist), `/amateur-circuit` (prospects). Five sites: (1) `templates/prospects.html` deleted (-111 lines), (2) `routes.py:1632-1649` `/prospects` route handler removed (-18), (3) `templates/base.html:45-47` sidebar nav entry removed (-3), (4) `templates/roster.html:28` empty-roster CTA relabeled "Browse Amateur Circuit" pointing to `amateur_circuit` route (-1/+1), (5) `templates/training.html:37` empty-state link relabeled "Sign amateurs →" pointing to `amateur_circuit` route (-1/+1). Net diff: -135 / +2 = **-133 net lines**, 5 files (1 deleted). Commit `e373290`. Carry-forward: `bridge.get_scouting_data()` in `game_bridge.py` is now unused but stays alive — out of scope to clean, might serve future scouting features. **Negative-scope ships are rare and high-value** — they require adjacent work to make the removal safe first. Ship #14 + Ship #18 made `/amateur-circuit` good enough that retiring `/prospects` didn't lose anything. The cleanup is downstream of the foundation. **Filed:** `memory/sidebar_prospects_retirement_2026-05-03.md` (the diagnostic that led to retiring vs renaming, captures the three-issue analysis and Option-C reasoning). **Note on commit ordering:** the work was applied 2026-05-03 evening but the commit got missed at the time; caught and committed properly during Ship #20 cleanup 2026-05-05. Out-of-order ship numbering (#19 commits AFTER #20) is a tiny cost; honest history wins.

## Shipped 2026-05-08
- Rivalry persistence wire-up (verified, **Ship #24 — second ship of the world-gen redesign arc, foundational unblocker for Ship #25 sim-seed**): closes the gap discovered in Session 5's escape-hatch memo (2026-05-07). **Yesterday's memo's hypothesis was WRONG and is being honestly corrected in the institutional record.** The memo claimed the rivalry module had no serialization and needed ~80-100 lines added across `rivalry.py` + `game_state.py`. Pre-apply discovery for Ship #24 found that Rivalry/RivalryEvent/RivalrySystem **already had complete `to_dict`/`from_dict` methods** at `narrative/rivalry.py:185-202, 366-412, 926-953` — proper enum encoding via `.value`, backward-compat defaults for `peak_score`/`total_events`, full pair-key string encoding. The actual gap was wire-up only: `game_bridge.web_save` (line 1083 area) and `web_load` (line 1190 area) never called the existing serialization methods. **Net diff: +13 lines, single file (`game_bridge.py`).** Pattern mirrors `injury_system` precedent byte-for-byte — save adds `"rivalry_system": get_rivalry_system().to_dict() if RIVALRY_AVAILABLE else {}`, load restores via 8-line block gated on `RIVALRY_AVAILABLE and "rivalry_system" in data`, mutates the module-level singleton via `_rivalry_module._rivalry_system = RivalrySystem.from_dict(...)` (same mutation pattern as `reset_rivalry_system()` at rivalry.py:972), then re-registers with GameState. **Verified:** round-trip idempotent on synthetic 4-fight rivalry blob (3 fights A-vs-B → score 100 BAD_BLOOD + 1 isolated C-vs-D); backward-compat (slot3 has 32 keys, no `rivalry_system` key — restore block correctly skipped, empty system initialized cleanly); forward-compat (constructed save with rivalry data loads cleanly, counters + scores + fight history all preserved); JSON serialization clean (2119 bytes for synthetic blob). Commit pending. **Lesson preserved** in `memory/concept_pre_seeded_rivalries_design_2026-05-07.md` (memo updated with corrected diagnosis, original hypothesis preserved as "what was wrong"): before assuming a system needs serialization built from scratch, **grep for `to_dict` / `from_dict` first**. The escape-hatch memo could have been ~5 lines instead of ~200 if I'd done that check yesterday. **Implements** memory/concept_pre_seeded_rivalries_design_2026-05-07.md (Ship A portion). **Unblocks Ship #25** — sim-seed rivalries via `process_fight()` call in `_simulate_single_fight`. Trivial follow-up, ~20-30 lines, world_init.py only. **Remains open from world-gen arc:** Ship #25 (sim-seed); Session 2 MEDIUM items (champion narrative UI, retrospective news, camp records reflecting sim, module-availability fallbacks); injuries-during-sim ship.

## Shipped 2026-05-07
- **Session 5 of world-gen arc — pre-seeded rivalries (NO SHIP, escape-hatch memo only)**: discovery pass for the second HIGH item from Session 2 gap list (Sandman-direct). Rivalry API at `narrative/rivalry.py` is clean — `FightContext` maps cleanly from `SimulatedFight`, `RivalrySystem.process_fight()` is the right entry point, performance non-issue (~3500 fights × O(1) call). But Decision 4 surfaced a foundational gap: **`GameState.to_dict()` (game_state.py:1193) and `from_dict()` (1245) skip rivalry state entirely.** `RivalrySystem` is a module-level singleton (`narrative/rivalry.py:_rivalry_system`), wired at runtime via `register_rivalry_system()` but never serialized. Routes.py doesn't touch it. game_bridge only reads. **Pre-existing gap: even today, runtime rivalries die on every Flask restart.** Sim-seeding without addressing persistence would surface a regression — fresh world looks lived-in on first load, all rivalries gone on second. Triggered the prompt's explicit escape hatch ("If RivalrySystem state isn't persistent — STOP, file memo, don't ship"). Honest scope is TWO ships: **Ship A (FOUNDATIONAL)** — add `to_dict`/`from_dict` to `Rivalry`/`RivalryEvent`/`RivalrySystem` dataclasses, encode enums via `.value`, wire into `GameState` serialization with backward-compat default for legacy saves; ~80-100 lines across `rivalry.py` + `game_state.py`. **Ship B** — inline `process_fight()` call in `_simulate_single_fight` for sim-seeded rivalries; ~20-30 lines, trivial after Ship A. **Bonus finding:** `narrative/rivalry.py` and `systems/rivalry.py` are byte-identical (1225 lines each) — same parallel-fork pattern as `game_state.py`; harmless today but flagged for future cleanup. Filed at `memory/concept_pre_seeded_rivalries_design_2026-05-07.md`. **Discipline note:** memo-first when foundational surfaces appear, same pattern as Sessions 1-3 of the aging arc. Refusing the wrong ship is the session's value. Recommended Session 6 = Ship A (rivalry persistence, foundational). Session 7 = Ship B (sim-seed, trivial follow-up). Or Van pivots to Session 2's MEDIUM items first — design surface is documented enough to make that call.
- World-gen aging-during-sim (verified, **Ship #23 — first code change of the world-gen redesign arc**): hooked the existing runtime aging system (`aging.py`) into `HistorySimulator`'s main loop so fighters age, decline, and retire during the 130-week sim instead of staying frozen. Sessions 1-3 of the arc were memo-only (audit + gap list + design discovery). Session 4 = first ship. **Locked decisions:** (1) annual aging tick every 52 sim-weeks (`week % 52 == 0`); (2) injuries DEFERRED to a future ship; (3) hybrid retirement — annual roll plus post-fight roll on lose-streak ≥3; (4) minimal champion protection (existing aging.py -0.15 modifier, no other special-casing); (5) replacement prospects spawn at retirement-fill rate, capped 1:1 same-division; (6) prospective direction (forward-only). **Foundational discovery during apply (Lock-4 surface):** `aging.py:271` calls `get_config("aging.retirement_avg", 38)` with two args, but the web-app's `core/config.py` stub only accepted one — every retirement probability call raised TypeError silently swallowed by my defensive try/except, masking 0 retirements. Fixed the web-fork stub by widening signature to `(path: str = None, default: Any = None)` matching the top-level `core/config.py` exactly; also fixed nested-path resolution (`return default` when key not found instead of returning empty `{}`). One additional file in the diff (`cage_dynasty_web/core/config.py`), 4 lines net, backward-compatible. **Architecture changes (single primary file `world_init.py`):** (a) `GeneratedFighter` gains `is_active`, `retirement_age`, `retirement_week` (all defaulted, no field-ordering risk); (b) `BeltHistory.vacate_belt(weight_class, week, event_name, reason)` ends current reign without crowning successor — clears `current_champions[wc]`; (c) `BeltHistory.crown_initial_champion` gains optional `won_method` param (defaulted to `"Inaugural Champion"`, used as `"Won Vacant Title (METHOD)"` for vacant-belt winners) — no new schema field, reuses existing `won_method` column per Lock 2; (d) `_simulate_single_fight` else-branch (formerly the "shouldn't happen" edge case) now properly records vacant-title wins into BeltHistory; (e) `_is_fighter_available` returns False for `is_active=False`; (f) `update_rankings` filters retired fighters out of division pools; (g) `_build_event_card` main-event scan now prioritizes `vacant_divisions` set FIRST — top-2 ranked fight for the vacant belt before falling through to existing title/ranked logic; (h) `_process_annual_aging(year, current_week)` walks active fighters at `week % 52 == 0` — increments age, applies attribute decline via `aging_system.apply_annual_decline()`, recomputes skill_rating, rolls annual retirement via `calculate_retirement_probability()`; (i) `_maybe_retire_post_fight(loser, week, event_name)` early-returns unless lose-streak ≥3, then rolls; (j) `_retire_fighter` marks `is_active=False`, populates retirement metadata, and on champion retirement calls `vacate_belt()` + adds to `vacant_divisions` set; (k) `_spawn_replacement_prospect(weight_class)` calls `FighterGenerator.generate_fighter(skill_tier="developing", age_range=(21,25))` and adds to `self.fighters`; (l) `_place_prospect_in_camp` minimal helper — walks same-division camps with ≥1 fighter in division, filters to those under MAX_PER_DIVISION=2, picks lowest-count camp; falls back to camp_id=None if all saturated; (m) `HistorySimulator.__init__` now accepts `camps` and `fighter_gen` params (both Optional); `WorldInitializer.simulate_history` passes them through. **Lock-4 finding:** `assign_fighters_to_camps` is monolithic (closure-scoped `camp_division_counts` dict + 3 inner helpers) — no extractable per-fighter helper. Built minimal 10-line placement helper accepting MAX_PER_DIVISION=2 duplication as known carry-forward. **Verification numbers (seed 42, 120w sim, vs Session 1 baseline ~3500/260/61):** total fighters 298 (was ~289 initial + 9 replacements); total fights 3248 (was ~3500, 7% drop expected from retired-fighter unavailability); title fights 240 (was ~260); belt changes 60 (was ~61, basically unchanged); retirements during sim 9; replacement prospects 9; champion retirements 0 at 120w (timeline too short for elite-cohort age cascade — confirmed via 312w/520w seed sweeps where champion-retirement vacate path fires cleanly: seed 99 → 4 champ retirements / 4 vacate events / 4 vacant-title wins, seed 314 → 7 champ retirements / 7 vacate events, seed 2025 → 1 champ retirement); avg active age 27.3 → 29.0 (aging is happening); avg retirement age 34.6 (just past retirement_min=34, makes sense for first wave). **Cadence stays near 12x reality** — that's the expected behavior for aging-alone per Session 3 lock #2; injuries-during-sim is the next ship for that finding. **Lineage integrity verified:** every retired-champion has a `lost_method='Retired'` reign-end event AND a corresponding `Won Vacant Title (METHOD)` reign-start event; 0 post-retirement booking violations across all sweep seeds. **Lock-1 verified:** lose_streak resets to 0 on a win (existing `_simulate_single_fight:1278` behavior, not touched by this ship). **Lock-3 verified:** zero retirements in a tick = zero replacements; division populations stable (initial counts = total counts now, retirements offset by 1:1 replacements). Net diff: +282 / -32 = **+250 net lines** across 2 files (`world_init.py +250`, `core/config.py +0` net for stub widening). Commit pending. **Implements:** `memory/concept_aging_during_sim_design_2026-05-05.md` (kept open as reference for future injuries-during-sim ship that will share design space). **Remains open from Session 2 gap list:** HIGH — pre-seeded rivalries (next ship in arc); MEDIUM — champion narrative UI surfacing, retrospective news, camp records reflecting sim, module-availability fallbacks; injuries-during-sim ship (deferred per Session 3 lock #2). **First code change of the world-gen redesign arc.**

## Shipped 2026-05-05
- **Diagnostic session — W/L counter bug investigation (NO-SHIP, not numbered)**: investigated `memory/bug_free_agent_player_fight_record_not_incremented_2026-05-05.md` filed alongside Ship #22. Original hypothesis ("same family as Ship #22, gated on camp_id") **confirmed WRONG** by static code analysis — all record-increment sites in `game_bridge.py` (line 2486-2487 fallback, line 6796 AI flow, line 8891 real engine) are unconditional. Adding a fix to remove a gate that doesn't exist would have been a no-op or worse. Symptom re-confirmed: Alex Harris stuck at 7-4-0 across 4 player-booked fights despite unconditional increment paths. Mechanism unknown — five candidate mechanisms remain in play (object identity drift, silent exception, serialization issue, wrong path, periodic reconciliation pass). **Adjacent Finding B surfaced:** `fight_history` mirror is incomplete for camp fighters too — Wei Martin has 4 entries vs 6 completed fights. Distinct from Finding A but likely same root cause; same diagnostic session can probably surface both. Pre-#20 backup file flagged as integrity-compromised (Alex's fight_history contains wk 43/50 entries despite saved week_number=40 — file is partially-overwritten autosave, NOT a clean pre-#20 baseline). Memo rewritten with: corrected diagnosis, original hypothesis preserved as "wrong" for honest history, five candidate mechanisms, runtime probe protocol with specific print statements + step-by-step observation procedure, don't-backfill-history discipline note. **No code changed. Refusing the wrong fix is the session's value.** Discipline note: stopping the wrong fix from shipping + upgrading the memo so future-pickup is faster — both real outputs even without a ship number. The arc total stays at 22.
- Free-agent booking UX (verified by code-read, **Ship #22 — closes the Alex Harris confusion class surfaced by Ship #20 measurement**): when player books a free agent via challenge or offer, two UX gaps existed. (1) Dashboard scheduled-fights rendered the free-agent side as "?" because `fight.fighter` is unset for non-camp fighters — player saw a phantom opponent with no identity. (2) After fight resolved, `_apply_post_fight_camp_record` gated all post-fight player news on `(is_player_fighter OR camp_match)`, both False for free agents — no headline fired, fight resolved silently, fighter disappeared from dashboard with zero narrative beat. **Fix:** dashboard now renders the free-agent name linked to `/fighter/<id>` (existing fighter profile route has a Challenge button — completes re-engage loop without adding new routes) plus `📋 Free Agent` chip in display-font matching Ship #17's emoji idiom. New `elif` branch in `_apply_post_fight_camp_record` fires `📋 Free agent {Name} won for you over {Opp} at {Event} — back in the pool, available to challenge again.` (win) or `dropped one to {Opp}` (loss). Gate: `is_player_fight=True AND not ftr.camp_id` — both conditions required, verified four ways (AI-vs-AI / camp fighter / free agent / opponent loop iteration). Does NOT bump `_player_camp_wins/losses` (those are camp stats; free agents don't belong). Category: `contract` (extends Ship #17's 📋 Free Agents tab). **Bonus catch:** the "?" display bug was pre-existing and worse than original diagnosis suggested — Alex Harris was never visible by name on dashboard while pending. Closed as side effect. Net diff +29/-1 = +28 lines across 2 files (`game_bridge.py +19`, `dashboard.html +10/-1`). Commit `9670864`. **Filed alongside (NOT fixed this ship):** `memory/bug_free_agent_player_fight_record_not_incremented_2026-05-05.md` — fighter's own pro record (wins/losses fields) doesn't increment on player-booked free-agent fights (Alex Harris stuck at 7-4-0 across 4 fights); distinct from camp record (correctly not bumped); same family as Bug O / R / S two-path-merger pattern; ~30-60 min separate fix arc when picked up. **Closes** `memory/todo_free_agent_booking_lifecycle_ux_2026-05-05.md`.
- Sub-ship B — helper consolidation (verified, **Ship #21 — extracts shared `_book_title_fight()` helper, routes four slice-helper booking sites through Ship #20's lead-time + queue + handoff infrastructure**): closes the gap left by sub-ship A. Ship #20 integrated lead-time into the AI booker's main flow only; five other booking sites (slice helpers from the champion injury arc) still wrote directly to `_upcoming_cards` using next-open-slot logic, bypassing the 8-12 week title-fight protection. Sub-ship B unifies that surface. New helper `_book_title_fight(wc, fixed_fighter, exclude_ids, apply_lead_time, decline_context)` derives vacant-vs-mandatory mode from whether `fixed_fighter` is passed (eliminates impossible state of mode-string mismatch). Four call sites rewired: Slice 0.75/2 auto-vacate at `:1747`, Slice 4 mandatory return at `:1567`, Slice 2.5 decline path at `:3204`, Slice 3 player vacate at `:3503`. Three old helpers deleted: `_book_vacant_title_fight`, `_book_mandatory_return_defense`, `_book_vacant_title_ai_fallback`. `_preview_vacant_title_contenders` parameterized with `min_contenders` (1 for mandatory return, 2 for vacant) and `require_open_slot` (False when caller computes target_week via `_weeks_out_for_fight`). **Latent bug fixed during apply:** preview's contender filter previously scanned `_scheduled_fights` and `_upcoming_cards` but NOT `_ai_deferred_bookings`. After Ship #20 introduced the queue, helper sites calling preview could re-pair fighters already booked by the booker. Added queue scan to `_all_booked` set construction — defensive fix aligned with sub-ship B's strict-invariant goal. Slice 2.5 accept path explicitly untouched (lead-time was applied at offer creation; re-applying at acceptance would double-process). Player-fight injection block at `:7935-7957` NOT relocated — different surface, different risk profile, deferred to future ship per `tech_debt_player_fight_injection_location_2026-05-04.md`. Net diff +166 / -111 = **+55 net lines**, single file. Commit `e3fa76a`. **Closes** `memory/tech_debt_preview_vacant_contenders_min_count_2026-05-03.md` (min_contenders parameterization) and `memory/tech_debt_title_strip_logic_duplication_2026-05-02.md` (helper sharing). **Remains open:** `tech_debt_player_fight_injection_location_2026-05-04.md`, `tech_debt_vacant_title_offer_indefinite_pending.md`, `tech_debt_vacant_title_offer_purse_hardcoded.md` (offer system, separate). **Post-Ship-#20 measurement pass still pending** — sub-ship B does not change main-path booker behavior, so the cadence + ghost-tier measurement is independent and should run as planned.

## Shipped 2026-05-04
- Sub-ship A — AI booker lead-time integration (verified, **Ship #20 — Monday's foundational ship · largest single ship of the four-day arc at +156 net lines**): fixes the architectural drift where the AI card builder bypassed `_weeks_out_for_fight()` entirely. Function existed at `game_bridge.py:5500-5518` with rank-aware lead times (8-12 weeks title, 5-9 ranked, 3-5 unranked) and was used correctly by player offer/challenge negotiation paths, but `_build_card_for_week`'s four paths (title / ranked-vs-ranked / lone-ranked-vs-unranked / all-unranked) all converged on `_make_scheduled_fight` with `target_week` passed in by `_top_up_pipeline`'s 6-week loop directly — no rank-aware lead-time computation. Result: title fights at 1-6 week leads (vs intended 8-12), Gabriel Lima 6-week metronome (deterministic exhaust of rolling pipeline, not deliberate spacing), ghost tier never reaching candidate list. Single architectural finding emerged from six diagnostic investigations: card capacity audit → matchmaker mechanism diagnostic → multi-week scheduling verification → target_week trace → architectural design conversation → Phase 2.5 implementation plan. Three diagnosed symptoms (champion cadence, ghost tier exposure, ranked allocation bias) collapsed to one upstream cause. Architectural changes (single file, `game_bridge.py`, +185/-29 = +156 net lines): (1) New `_ai_deferred_bookings` structure with strict hand-off invariant (entity in EXACTLY one of three sinks at any time — `_scheduled_fights` / `_upcoming_cards` / `_ai_deferred_bookings`); persistence via to_dict/from_dict (defaults to [] for legacy saves, no migration). (2) `_build_card_for_week` paths 1-4 each compute target_week via `_weeks_out_for_fight`; universal card-fill loop routes by `computed_target_week == target_week` — same-week placements go on this card, different-week placements defer to queue with 📅 [LEAD-TIME QUEUE] log. (3) `_top_up_pipeline` restructured to three phases: cancellation re-eval (drop queue items with broken pairs, 🚫 [LEAD-TIME CANCEL]), hand-off (drain queue items whose target_week entered the 6-week window, 📅 [LEAD-TIME HANDOFF]), build/extend (call `_build_card_for_week` per pipeline week with always-call semantics — idempotent because Site 4's reuse-existing logic preserves drained fights). (4) `CARD_TARGET_FIGHTS = 9` module constant — shared between `_build_card_for_week` target_count, Phase 2 capacity gate, and load-time auto-truncate. (5) Helper extraction: `_create_empty_card_dict` shared between `_build_card_for_week` and `_top_up_pipeline` Phase 2. **Three regression fixes bundled (caught by verification):** (a) Pre-existing scheduled-fight injection block at `_build_card_for_week:7935-7957` needed dedup — block was idempotent pre-sub-ship A because cards built only once; Phase 3 always-call surfaced the latent gap, and every advance_week rebuild re-appended the same player fight. Verified on slot3: Wei Martin vs Scott Thomas appeared 4× on DFC 46 after 3 advance-weeks. Fix: dedup using fight_id set membership. (b) Phase 2 hand-off lacked capacity gate — queue items accumulated across multiple advance-weeks (each advance generates new candidates routing to queue via lead-time function). When target weeks entered the rolling window, all queued items drained onto the same card — DFC 45 reached 29 fights, DFC 46 reached 21 pre-fix. Fix: capacity check in Phase 2 with ⚠️ [LEAD-TIME OVERFLOW] log; dropped queue items become eligible in next advance's candidate generation. (c) Auto-truncate on load via `from_dict` to clean existing bloated state — sorts by `(player_first, slot_priority, -score)`, keeps top 9; player fights protected from truncation regardless of slot/score; fires once on load if any cards over capacity, idempotent thereafter, 🔧 [CARD TRUNCATE] log. Also added [LEAD-TIME DISPLACED] log for same-week card-full drops. Commit `73d2cd2`. Verified clean on slot3: cards composing within capacity (DFC 48-53 at 7-10 fights), Wei Martin appearing once on DFC 46, all four lead-time log lines firing correctly, no regressions on rankings/news/training/ladder, save/load round-trip clean. **Carry-forward:** Path-3 silent-fail (Alpha) preserved as known issue; Slice 0.75/2/2.5/3/4 booking helpers continue using `_upcoming_cards` direct-write (sub-ship B's natural touch); player-fight injection at 7935-7957 belongs in `_top_up_pipeline` Phase 2 architecturally (memo: `tech_debt_player_fight_injection_location_2026-05-04.md`); Phase 2 prioritization is FIFO, could be slot-aware if needed. **Sub-ship A is foundational** — sub-ships B (helper consolidation), C (player visibility for queued fights), D (decision window backend + UX) build on this infrastructure. **Closes finding:** `memory/finding_ai_booker_bypasses_lead_time_function_2026-05-04.md`. **Implements plan:** `memory/phase_2_5_lead_time_aware_scheduling_plan_2026-05-04.md`. **Filed alongside:** three new TODO memos surfaced during verification — `todo_dashboard_fighter_availability_indicator_2026-05-04.md` (UX), `todo_dashboard_training_report_in_depth_2026-05-04.md` (UX), `todo_tune_injury_rate_down_2026-05-04.md` (tuning). Plus `bug_r4_finish_on_prelim_slot_2026-05-04.md` (pre-existing R4 prelim symptom, sub-bug O.1 family, not sub-ship A related). Post-ship measurement pass on slot3 (advance 5-10 weeks, re-run cadence + camp_id ghost split) determines whether Beta (penalty reduction) and Alpha need separate ships.

## Shipped 2026-05-02
- Champion injury Slice 2 (shipped, code-reading verified): vacate-on-career now auto-books a vacant-title fight at the next available card. Pre-existing auto-vacate at `game_bridge.py:1460` (Slice 0.75 threshold) stripped the belt and fired 👑 TITLE VACATED news but didn't schedule a successor fight — divisions remained headless until manual intervention. Inline +71-line block inserted between strip completion (line 1484) and the autosave section (line 1486), still inside the `for wc` loop and `if weeks_injured >= 25:` block. Fires BEFORE line 1490's `_update_all_rankings()` so `div.rankings` still reflects pre-vacate state (ex-champion not in the list). Filtering predicates mirror existing card-builder patterns: active fighter, not the ex-champion (defensive), `camp_id != player_camp_id` (Slice 2 is AI-only), not in `_all_booked` set (computed from `_scheduled_fights + _upcoming_cards`), `is_cleared_to_fight`. Walks `div.rankings[:8]` to allow fallback past unavailable top-2; breaks at 2 contenders. Card selection scans `_upcoming_cards` chronologically for first card without an existing main_event slot — guarantees no double main events. Booking via `_make_scheduled_fight(top1, top2, wc, event_name, target_week, "main_event", is_title=True)`, append to `card["fights"]`. News fires `🏆 VACANT TITLE FIGHT: {top1} vs {top2} for the {wc} belt at {event_name}.` immediately after the existing 👑 TITLE VACATED entry. Edge cases gracefully degrade with terminal warnings (deferred booking, not crashes). Commit `b4ac430`. Verified by code-reading on slot1 — 8 weeks advanced, no champion hit ≥25w injury this cycle so the booking path didn't trigger; Slices 0.5/0.75/1 all firing correctly with no regressions. Filed Slice 2.5 (player vacant-title invitation prompt) at `memory/slice_2_5_player_vacant_title_invite_2026-05-02.md` — when player is ranked #1/#2 in a vacating division, Slice 2's AI-only filter passes them over; needs dashboard-blocking prompt to respect player agency. Same family as Bug Z. Bundles cleanly with Slice 3's prompt infrastructure when picked up.
- Bug AB.1 (verified): watch_fight FOTN banner spillover. `templates/watch_fight.html:43` was the fourth and final FOTN per-fight rendering surface still reading `fight.is_fotn` directly — old DFC 1-30 fights all showed the banner due to slot3's pre-commit-56bf807 corrupt is_fotn=True writes. Fix required ROUTE-level change because watch_fight handler at `routes.py:2010-2067` only loaded `fight_result` from `_completed_events`, not the containing event. Route now captures `fight_event = ev` alongside `fight_result = f` in the same loop iteration (line 2019), passes `event=fight_event` to render_template. Template gate switched from `{% if fight.is_fotn %}` to `{% if event and event.fotn and event.fotn.fight_id == fight.fight_id %}` — defensive `event and` guard for hypothetical None paths, mirrors Bug AB three-template pattern. Net diff: 4 lines across 2 files (3 in routes.py, 1 in template). Commit `7914647`. Verified across slot3 (week 38, both old corrupt and new clean events): old DFC 17-29 watch-fight URLs show no banner (self-fix via `event.fotn=None` for legacy events), new event watch-fight where fight WAS FOTN winner shows banner correctly, new event watch-fight where fight was NOT FOTN winner shows no banner. Scorecard/commentary/rounds rendering unchanged. **Closes the four-surface FOTN derivation family** — event_detail (Bug AB), week_results (Bug AB), archives (Bug AB), watch_fight (Bug AB.1) all source from `event.fotn.fight_id == fight.fight_id`. Single source of truth. Corrupt `fight.is_fotn` flag persisted in slot3 events 1-30 is now truly dead read data.
- Champion injury Slices 3+ remain queued: Slice 3 (player decision UX with dashboard prompt for Severe injuries on player's own champion + naturally bundles Slice 2.5's player vacant-title invitation), Slice 4 (hold-path consequences — cardio decay, mandatory return defense, news tone progression), Slice 5 (interim belts + cleanup of vacate headline text), Slice 6 (ring rust + polish). See `memory/design_champion_injury_handling_2026-04-30.md`.
- Earned-nickname feature (verified, **above original investigation scope**): nicknames must be EARNED through performance, not assigned at creation. Threshold: ≥2 wins AND ≥3 total fights, winner-only check. On post-fight resolution where the winner crosses the threshold with no nickname, a random pick from a 230-entry curated pool is assigned + a 📢 news headline fires (`{Fighter Name} adopts the nickname "{Nickname}".`) + terminal log with record context. One-shot backfill in `web_load` applies the same threshold to legacy fighters so existing saves light up retroactively without invalidating prior decisions. Six-site bundle: `game_state.py:NICKNAMES_POOL` (230-entry curated pool across 9 categories — Originals, Warrior/Weapon, Animal Kingdom, Natural Phenomena, Personality/Traits, Physical Attribute, Technical/Skill, Mysterious/Intimidating, Short/Flavor — 0 Nones, threshold IS the no-nickname gate); `_generate_fighter` reverts the prior tier-1 creation-time assignment; `game_start.py` and `routes.py` revert their tier-1 assignments + imports; `web_load` backfill gains threshold gate; `_apply_post_fight_camp_record` gains the post-fight earning hook inside the existing winner+loser for-loop. Pool dedupes applied: "The Beast" and "The Technician" already in originals; "The Octopus" and "The Void" duplicated within new sections; "Pitbull" stylistic dup of original "The Pitbull". Net diff: +149 lines across 2 files (the other 2 files reverted to HEAD). Commit pending. Verified on slot3: ✨ [NICKNAME BACKFILL] 145 fighters earned nicknames retroactively; James Anderson earned "The Verdict" mid-session (going-forward path verified); Wei Martin "The Axe" rendered in style-accent red, Lucas Adesanya "Swift" rendered correctly; raw 0-0 prospects stay nameless. **Closes `memory/nickname_population_investigation_2026-05-02.md`.** Original investigation diagnosed dormant pipeline; design pivoted mid-session from "fix the gap" to "ship the earned mechanism with curated pool" — Van's instinct that random-at-creation cheapens nicknames was right. Eleventh ship of the two-day arc.
- Fighter profile polish (verified): Leather-style screenshot-worthy hero + 4-quadrant stats grid. Five components, two files (`static/css/style.css`, `templates/fighter_profile.html`), +88/-28 lines. (1) New CSS tokens `--violet` and `--teal` (plus -dark variants) — violet for hybrid-style class banners, teal for Mental stat-block accent, both avoiding gold collision. (2) Style class data map: 17 fighting-style entries with family classification (striker/grappler/hybrid) and 3-5-word tagline; defensive default for unrecognized styles. (3) Hero block restructure: full-width style-class banner before `<h1>` (gradient bg + 3px accent border + icon + style name + right-aligned tagline = RPG class header); nickname promoted from italic 1.1rem muted to 1.5rem display font in style-accent color with gold quote marks (broadcast-graphic convention); old gray style chip below record removed. (4) Stat-block macro accent parameter: 3px left border + accent-colored display-font header per category (Striking=blood-red, Physical=warning, Grappling=info, Mental=teal); inner per-row tier coloring unchanged. (5) Record format: `12 (8KO · 4SUB) – 3` → `12(8) – 3(1) – 0` broadcast format with `ko_l` Jinja `selectattr` chain on `fight_history`; verbose method-splits subtitle gated on `wins > 0`. Commit `f9c2a07`. Verified on slot1 across striker (Wei Martin Pressure Fighter — red banner + "Walks you down"), grappler (Tai Whittaker Wrestler — blue banner + "Takes you down. Keeps you there."), hybrid (Benjamin Taylor Sprawl & Brawl + Francisco Ortiz champion — violet banners). Champion gold top bar still renders correctly without conflicting with the new banner. CLAUDE.md "Current top-of-list" item 2 marked DONE.
- Filed: nickname population investigation. Nicknames never observed populated in slot1/slot3 despite the new UI rendering support — feature is dormant data, not broken UI (`{% if fighter.nickname %}` guard handles cleanly). Two-step audit needed: (1) is nickname generation implemented at fighter creation? (2) does the field map engine→web correctly? Design question surfaces if implementing: hybrid recommendation (low-key nicknames from pool at creation, distinctive on milestones) — but call goes to Van. See `memory/nickname_population_investigation_2026-05-02.md`.
- Champion injury Slice 3 (verified, **largest single commit of the arc**): player vacate-or-hold decision UX. When a player champion takes a Severe (9-24w) injury, advance_week is blocked until the player visits `/champion-injury/<fighter_id>` and chooses. Vacate strips the belt + books vacant-title fight (via shared helper) + redirects. Hold records to `_champion_holds` (Slice 4 reads for cardio decay). Career-level (≥25w) skips Slice 3 entirely — Slice 0.75 auto-vacate handles those. Detection helper called from all three injury sites (training/fight-score-fallback/fight-real-engine), gated on player+champion+9-24w; idempotent. Two new bridge fields (`_pending_injury_decisions`, `_champion_holds`) round-trip via to_dict/from_dict. advance_week guard returns `blocking_decisions` payload; routes redirect to first pending decision. Heal sweep at end of advance_week pops `_champion_holds` entries when `return_week` reached (silent pop; Slice 4 hooks here). New decision page `templates/champion_injury_decision.html` shows two-column preview (vacate path = vacant-title fight contenders + target card; hold path = return week + cardio-decay placeholder). Three new routes: GET /champion-injury/<fighter_id>, POST .../vacate, POST .../hold. Sequential UX — each decision page handles one fighter, redirects to next pending or dashboard. Net diff: +425 / -77 across three files (`game_bridge.py +196`, `routes.py +69`, new template ~160). Commit `2c13d0a`. Verified via smoke-test (save/load round-trip clean, all routes 200, no regressions). Slice 3's actual flow won't naturally trigger this session — same compound-rare-trigger reality as Slices 2 and 2.5; surfaces naturally when player gets a champion who takes a Severe injury. **Closures bundled in this commit:** (1) `tech_debt_slice_2_2_5_contender_pick_duplication.md` RESOLVED via shared `_preview_vacant_title_contenders` + `_book_vacant_title_fight` helpers — three callers (Slice 2 auto-vacate, Slice 2.5 decline fallback, Slice 3 player vacate) now share the contender filter + card scan logic; refactor net negative diff (~-15). (2) Slice 5 vacate headline cleanup RESOLVED inline — Slice 0.75's "due to injury — interim title fight to be announced" replaced with "during recovery." matching Slice 3's vacate news; closes the Slice 5 sub-task amended into the design spec. **Filed as new tech debt:** title-strip mutation pattern duplicates between Slice 0.75 auto-vacate and Slice 3's `resolve_champion_injury_decision` vacate branch (third site likely Slice 4's mandatory-defense work). See `memory/tech_debt_title_strip_logic_duplication_2026-05-02.md`. **Twelfth ship of the two-day arc.**
- Champion injury Slice 2.5 (shipped, code-reading verified): player vacant-title invitation. When a champion takes a Career-level injury and the player owns the #1 contender (and not also #2), fire a vacant-title offer through the existing /offers system instead of Slice 2's AI-only skip. Player accepts → title fight schedules with `is_title_fight: True` (via bundled accept-handler latent-bug fix at `game_bridge.py:2925`); declines → AI #1 vs #2 fallback books immediately into next open main slot with "(player declined the shot)" news; multi-fighter at #1 AND #2 falls through to AI #3 vs #4. Three components in `game_bridge.py`: player-detection block (~85 lines, inserted at line 1502 in vacate logic), `decline_fight_offer` extension (+5 lines), new `_book_vacant_title_ai_fallback` helper (+60 lines). Plus the accept-handler one-line fix. Net diff: +170/-4. Commit `33c8225`. Three tech debt memos filed alongside: contender pick + card scan duplication (`_book_vacant_title_ai_fallback` mirrors ~95% of Slice 2's inline logic), offer expiry (vacant-title offers sit indefinitely if player ignores), purse hardcoded (100k/50k should scale per `_maybe_generate_inbound_offers` formula). All three deferred to next-touch on either booking site or the offer system.

## Shipped 2026-04-30
- Bug Z (verified): auto-booking after player gets ranked. `_build_card_for_week` ranked-vs-ranked pairing at `game_bridge.py:7326-7368` iterated `division.rankings[:14]` with no player-ownership guard — once a player fighter entered top 14 of any division, the auto-builder picked them into upcoming events without negotiation. Fix: one-line filter `(not player_camp_id or f.camp_id != player_camp_id)` added to availability list at line 7295. `player_camp_id` was already grabbed at line 7275 and previously unused. Belt-and-suspenders form so the filter no-ops cleanly if camp isn't yet established (Optional[str] = None at game_state.py:336). Commit `59193c5`. Verified: Wei Martin (player, #3 FLY) won SPLIT DEC at DFC 28; post-fight `/ladder/Flyweight` showed cooldown only, no auto-booking. Other 11 ranked FLY fighters auto-paired into DFC 29-34 as expected — filter is correctly selective. Same two-path-merger family as Bug O / Bug R / Bug Q / Bug S — system that should treat player and AI fighters differently but didn't.
- Filed: judge-tendency observation (grappling rounds possibly undercredited) — n=1 split decision where player controlled all 3 rounds via takedowns/back mounts/RNCs and 2 judges still scored against. Park, don't act. See `memory/judge_grappling_tendency_2026-04-30.md`.
- Sub-bug audit (S.1/S.2/S.3) closed — bundle diagnosis on `_run_real_engine` asymmetries vs other fight paths. Verdicts: **S.1 DEFER**, **S.2 KILL**, **S.3 KILL**. No code shipped. Key findings: (1) S.1 — `_run_real_engine` doesn't call `_update_rankings_after_fight` per-fight, but `advance_week:1471` calls `_update_all_rankings()` once per week which iterates every division — Bug E/G's verified rank-delta path runs through this bulk refresh, so the asymmetry is intentional coverage, not a gap. (2) S.2 — `_fighter_data['fight_history']` mirror is read only at lines 3050/6950 as fallbacks when canonical `fighter.fight_history` is empty; canonical is never empty post-Bug-S, so the mirror is functionally dead code. (3) S.3 — `_run_real_engine:7987` calls `_apply_post_fight_camp_record` which inserts `"category": "player_result"` news at lines 1914/1923 — player fights DO emit news, just under a different category by design. Same logic as Bug AA downgrade — not shipping defensive code for hypotheticals.
- Bug AA downgraded to architectural-gap-no-live-reproduction (covered separately above) and `challenge_fighter` harmless-gap filed — see commit `ba163fe`.
- Filed: pattern observation — diagnose-first discipline saved ~30+ minutes of speculative fix work on the S.1/S.2/S.3 audit; 2 of 3 filed concerns turned out to be misunderstandings of intentional design (S.3 categories) or coverage by a different mechanism (S.1 bulk refresh). Asymmetry between code paths ≠ bug; check coverage, intent, and dead-code status before proposing symmetry fixes. Trust prior verifications. See `memory/pattern_diagnose_first_saves_speculative_fixes_2026-04-30.md`.
- Filed: champion-injury-handling design spec (multi-session feature). Severity-tiered: Minor/Moderate (≤9w) auto-hold; Severe (9-24w) player chooses vacate vs hold; Career-level auto-vacate. Vacate path → top-2 fight for vacant belt, player returns #1 with tune-up-then-title-shot logic. Hold path → belt holds with cardio decay and mandatory title defense on return. Decision UX is a dashboard-blocking prompt after the injury fight. Slices 0-6 sequenced for incremental shipping; Slices 0+1 (auto-book audit + injury news headline) are next-session start, not auto-launched. **Slice 0 awaits fresh prompt — do not auto-start.** See `memory/design_champion_injury_handling_2026-04-30.md`.
- FOTN top banner (verified): event_detail page now renders the gold-gradient FOTN banner above the slot grid for events that produced a Fight of the Night. `_build_card_for_week` selection block at `game_bridge.py:1516-1540` was setting per-fight `is_fotn` flags but leaving `event["fotn"]` at its `None` initialization (line 1272), so the existing banner template at `event_detail.html:27-47` was dead code. Fix: +13-line block after FOTN selection succeeds populates a 4-key dict `{fight_id, fighter1_name, fighter2_name, bonus}` onto whichever event in `_completed_events` for the current week contains the FOTN fight. Bounded scan handles all branches (merged ai_event, unmerged player events, no-card else path). Commit `4736de7`. No-op path preserved: when `fotn_result` is None (card had <2 fights or score below 30 threshold), event.fotn stays None and banner stays hidden. Existing dashboard FOTN news headline (verified 2026-04-27) still fires unchanged.
- Bug AB filed (NEW, NOT FIXED): per-fight FOTN badge spillover on event_detail. All fights on the page render the "🔥 FOTN" badge + gold left border instead of just the actual FOTN winner. Pre-existing bug (clear loop at `game_bridge.py:1519-1533` is byte-identical to its pre-ship state — diagnosis confirms our diff cannot have caused it), exposed visually now that the top banner draws attention to FOTN. Investigation targets next session: clear loop not running, clear loop writing to wrong object references, or template gating bug. Cheap diagnostic print sketched in memo. See `memory/bug_AB_per_fight_fotn_badge_spillover_2026-04-30.md`.

## Shipped 2026-04-29
- Bug O (verified): prelim player fights running 5 rounds. `_run_real_engine` at `game_bridge.py:7922` was setting `is_main=True` for any player fight regardless of `card_slot` — conflated "player participation" with "main event status." This forced 5-round FightConfig for all player fights (the asymmetric upgrade-only round override at `fight_integration.py:1228-1229` then locked it in). Fix: remove the `or fight.get("is_player_fight", False)` clause from line 7922. Player fights now respect their slot semantics. Commit `7c3f8e1`. Verified: Raj Panyawong (player, prelim) won SPLIT DEC after R3; AI prelims max R3; title fights still 5 rounds (Oscar Gane DEC R5). No regressions. Same two-path-merger pattern as Bug R, Bug Q, Phase 0 — fields/flags diverging between player and AI paths. AI path was already correct.
- Filed: Sub-bug O.1 (`fight_integration.py:1228-1229` asymmetric upgrade-only round override — currently harmless post-Bug-O, defense-in-depth cleanup deferred). Tech-debt note: `game_bridge.py:7923` redundant slot check after `is_main` is already computed at 7922 — cosmetic cleanup. See `memory/sub_bug_O1_engine_round_override_asymmetric.md` and `memory/tech_debt_game_bridge_7923_redundant_slot_check.md`.
- Bug S (verified): cooldown not applied to player-fight opponents. `_run_real_engine` returned without writing `fight_history` for either fighter; cooldown loop in `advance_week` (lines 1367-1375) calls `_apply_cooldown` → `_cooldown_weeks` → `_get_fighter_lose_streak`, which reads `fight_history`. With no L recorded, `lose_streak == 0` → fell into the WINNER branch → unranked losers got 1w cooldown instead of 4w. Player could spam-rematch any AI fighter they just beat. Fix: 15-line `fight_history.append()` block inside `_run_real_engine` (between contract decrement and scorecard), mirroring AI path at `_simulate_card_fights:6073-6081` byte-for-byte. Commit `c203b51`. Verified: Wk 23 DFC 23 — Raj beat Ulugbek; Ulugbek now shows "Available Week 27 (4w)" on LHW ladder. Pre-existing latent bug (not Option P regression). Same two-path-merger family as Bug O / Bug R / Bug Q / Phase 0.
- Filed: Sub-bug S.1 (missing `_update_rankings_after_fight` in `_run_real_engine`), Sub-bug S.2 (missing `_fighter_data` fight_history mirror), Sub-bug S.3 (possibly missing news-headline insert — verify in dashboard before classifying). All low priority; bundle when `_run_real_engine` is touched again.
- Bug Z filed (NEW, HIGH, NOT FIXED): auto-booking after player gets ranked. Raj (#11 LHW) auto-scheduled into DFC 17 vs Robert Lopez (#8) without negotiation. Player agency lost. See `memory/bug_Z_auto_booking_ranked_player_2026-04-29.md`.
- Bug AA filed (NEW, HIGH-ish, NOT FIXED): offer queue doesn't reconcile with scheduled fights. Inbound offer arrived for a fighter already booked; offer stayed accept-able. Sequence Z first, AA second — fix shapes may share. See `memory/bug_AA_offer_queue_doesnt_reconcile_2026-04-29.md`.

## Shipped 2026-04-28
- Phase 0: NameError fix in `_simulate_ai_fights_week`. Two latent `fight` references at lines 6229 and 6285 were causing silent engine fallback (line 6229 inside try/except — every AI fight via this path was silently using score-based fallback) and a crash that derailed yesterday's Bug E/G attempt (line 6285). Replaced with local `is_title` derivation and inline dict literal. Side effect: AI fights via this path now use the real engine. Commit `2e169e7`.
- Bug E + G (verified): timing alignment via Option P. Player-fight simulation block moved to AFTER the week increment so both player and AI fights tag with the same post-advance week. Merge logic at lines 1326-1344 now actually matches; player fight appears in DFC card lineup AND on its event's recap. Single structural move, no source_week ceremony, no FOTN tag change. Commit `2123ac7`. Verified on fresh game: Carlos Gonzalez fight at DFC 3 appears on Week 3 recap + DFC 3 card lineup; FOTN headlines tagged Week 3; rank deltas working; AI fights firing normally.
- Filed: Bug O (prelim round count) — Carlos Gonzalez prelim fight ran R4 (should max R3 for prelims). Filed for next session diagnosis. See `memory/bug_O_prelim_round_count_2026-04-28.md`.
- Bug Q (verified): AI fight watch links collision fixed. `_make_scheduled_fight:7481` used `f1.fighter_id[:8]` which truncated to the `'fighter_'` prefix (exactly 8 chars) — every AI fight on a given week resolved to identical URL `/watch-fight/fight_N_fighter__fighter_`. Fix: removed `[:8]` slices, use full fighter_ids matching player-fight ID convention. Commit `8c48417`. Caveat: past archived events retain broken IDs (no migration).
- Bug R (verified): player fight rendering on event detail page fixed. `_simulate_fight` built result dict without `card_slot`, so player fights merged into `ai_event.fights` via Option P had no slot attribute and fell through to the unslotted fallback render (flat string "TKO X def. Y"). Fix: add `card_slot` to `_simulate_fight`'s result dict, default `'prelim'` matching `_simulate_card_fights:6057`. Commit `e2360fc`. Pre-existing latent bug unmasked by Option P — same pattern as Phase 0's NameError.
- Filed: Bug T (amateur tournament system audit, NOT a fix task), Bug S (cooldown — HIGH priority), Bug Y (talent rarity rebalance — camp-start only, not pre-gen). See respective memory files.

## Shipped 2026-04-27
- Bug C (partial): champion-self-fight contender loop guarded — but a second code path still fires it (re-confirmed in play-test, see memory).
- Bug D (verified): media.py randrange crash fixed (`min(3, wins)` clamp).
- Injury rates tuned: 5-change calibration to `cage_dynasty/systems/injury.py` — verified ~5/wk new injuries, 0 new severe across 35+ fights.
- Bug F (verified): camp record + player-result headlines now update across all 4 fight paths via `_apply_post_fight_camp_record` helper.
- FOTN wiring confirmed already done (CLAUDE.md TODO #1 marked DONE).
- New design principle added: OVR is player-facing only, never engine input. See "Design principles" section below.
- is_title crash fix in `_maybe_generate_inbound_offers` (use-before-assign at line 1800; assignment moved up).
- OVR-out-of-rankings Phase 1 (verified): 7-diff refactor — rank_score formula no longer reads OVR, drops streak term, bumps recency + ranked_wins weights; min-fights threshold raised to 5/3; `best_rank` field on FighterRecord with returning-contender NEW_ENTRY_CAP exemption; re-rank-on-load via `bypass_clamp=True`. Verified on slot3 LHW ladder — high-OVR thin records correctly unranked, low-OVR veterans correctly ranked. Phase 2 (pre-gen world history) is the priority next session.
- Bug I — championship phrases firing on non-title fights: `commentary.py:LATE_ROUND_CONTEXT` content-cleaned (selector was already gating correctly; pool itself contained title-themed phrases). Two phrases swapped for neutral alternatives.
- Bug K — standing-finish commentary on grounded fighters: `commentary.py:FINISH_SEQUENCE["hurt_followup"]` content-cleaned. Two standing-only phrases ("WOBBLES! POUNCES!", "THE LEGS ARE GONE!") swapped for neutral alternatives. Structural fix (position-aware selector signature) deferred — see `memory/tech_note_finish_sequence_position_aware.md`.
- Bug L1 — champion injury indicator on rankings: `templates/rankings.html` shows "🤕 INJURED — Xw out" next to champion name when `champion.is_injured`. WebFighter already carries injury fields; pure template-side change. Visibility prelude to Bug L (interim title system) — interim infrastructure NOT built.

## Shipped 2026-04-26
- Dashboard digest: bare except replaced with logged version
- is_champion NameError patched in _convert_real_fighter
- Negotiation routes hardened with .get() guards (3 sites)
- Slot inflation Bug A: rank-floor now gated on matchup_credible

## Next session priority
**Bug AA — DOWNGRADED 2026-04-30.** Architectural gap confirmed at `game_bridge.py:1743-1758`
(reads `_scheduled_fights` only), but Bug Z's fix neutralized the live reproduction path —
no code path today writes player fighters into `_upcoming_cards`, so the gap is dead under
current code. Revisit only if a concrete failure surfaces or if a future feature reintroduces
a player-fight write to `_upcoming_cards`. Not shipping defensive code for a hypothetical.
See `memory/bug_AA_offer_queue_doesnt_reconcile_2026-04-29.md`.

**Filed: challenge_fighter same-family gap, harmless under current code.** `game_bridge.py:5166-5173`
reads `_scheduled_fights` only when validating challenge target. UI gates the Challenge button via
`scheduled_map` (routes.py:875-886) which reads both sinks, so unreachable in normal play. Defensive
note for future UI work — any new challenge entry point that doesn't reuse `scheduled_map` would
expose the gap. Same UI/backend predicate-source-divergence family as Bug AA. See
`memory/challenge_fighter_harmless_gap_2026-04-30.md`.

Then: Pre-gen world history (Phase 2 of OVR-out-of-rankings) — `_generate_initial_history`
in `game_state.py` is currently a trivial stub. Either build engine-driven pre-gen there
or wire up the dormant `world_init.HistorySimulator`. Decision needed at session start.

Also queued: Bug H (AI offer skips gameplan — adjacent to Z/AA, may bundle), Bug C second
path, Bug T (amateur tournament audit — not a fix, system verification), Bug Y (talent
rarity rebalance — camp start only, design questions first), SUB-rate undershoot tuning
(see `memory/sub_rate_undershoots_2026-04-28.md`).

Deferred low-priority cleanup: Sub-bug O.1 (asymmetric round override at fight_integration.py),
line 7923 redundant slot check, Sub-bug S.1 (rankings update missing in `_run_real_engine`),
Sub-bug S.2 (`_fighter_data` mirror), Sub-bug S.3 (news headline — verify symptom first).
Bundle whenever `fight_integration.py` or `_run_real_engine` is touched for another reason.
See `CLAUDE_NOTES/2026-04-26-slot-fix-followup.md`.

# CAGE DYNASTY — Claude Code instructions

## Project overview

Cage Dynasty is a browser-based MMA management simulation. Solo developer (Van).
Python/Flask web app at `~/Desktop/Games/cage_dynasty/cage_dynasty_web/`.
Deployment target: PythonAnywhere.

## North star

Emergent stories players tell unprompted. The reference is "Sandman" — an AI
rival in Leather (boxing management game) who fought the player three times,
became friends at feud Stage 30, never won a title, and got a player-written
story posted to Reddit. Nobody scripted it. The simulation made them care.
That's the bar.

## Design principles

**OVR is player-facing only, never engine input.** Overall Rating (`overall_rating`)
is a UI summary stat. It must NOT be used as input to any simulation system —
rankings, matchmaking, AI decisions (signing, contracts, retirement, trash talk),
fight outcomes, card slot assignment, title eligibility. Real MMA doesn't have
OVR. Rankings are earned through wins, recent form, and quality of opposition.
Engine work that needs a "strength number" must derive it inline from per-attribute
stats (striking, wrestling, cardio, etc.) — never reach for OVR as a shortcut.
See `memory/principle_OVR_player_facing_only.md` for full rationale and audit tactic.

## Architecture — VERIFIED April 2026 (do not trust other descriptions)

The web app is NOT a thin shim over the CLI engine. It is a substantially
independent codebase that shares a folder tree with the CLI by historical
accident.

**Live for the web app:**
- Flat .py files at `cage_dynasty_web/` root (`game_bridge.py`, `routes.py`,
  `game_state.py`, `fight_engine.py`, `fight_integration.py`,
  `card_builder.py`, `matchmaking.py`, `aging.py`, `amateur.py`,
  `facilities.py`, `maintenance_training.py`, `interviews.py`, `news.py`,
  `styles.py`, `world_init.py`, `name_database.py`)
- Stub packages: `cage_dynasty_web/core/`, `entities/`, `systems/` — small
  re-exporters that point back to the flat files above
- Top-level `cage_dynasty/simulation/` and `cage_dynasty/narrative/` —
  reached by accident: the symlinks `cage_dynasty_web/simulation` and
  `cage_dynasty_web/narrative` are BROKEN (relative target wrong), Python
  silently falls through to top-level via sys.path

**Dead from the web app's view:**
- Top-level `cage_dynasty/core/`, `entities/`, `systems/` — never imported
  by the web app. CLI tools use them. Web app does not.
- The CLI's `core/game_state.py` and web's flat `game_state.py` have
  diverged (~37 lines diff, drifted name generation). Sibling forks.

### Where to edit what

| Change | Edit this file |
|---|---|
| Game state, fighter records, name gen | `cage_dynasty_web/game_state.py` |
| Fight engine constants | `cage_dynasty_web/fight_engine.py` |
| Bridge / route handlers | `cage_dynasty_web/game_bridge.py`, `routes.py` |
| Card / slot assignment | `cage_dynasty_web/card_builder.py` |
| Matchmaking, training, aging | flat file at `cage_dynasty_web/` root |
| Templates | `cage_dynasty_web/templates/*.html` |
| Anything CLI-only | top-level `core/`, `systems/`, `entities/` |

### Known hazards

- Two parallel `game_state.py` files. Editing one does not update the other.
- Broken symlinks load top-level packages by accident. Don't "fix" them
  without verifying behavior first.
- Flat-first import loop: `from foo import x` finds `cage_dynasty_web/foo.py`
  before `cage_dynasty_web/foo/__init__.py`, before top-level. Adding a flat
  file with a name that collides with a package shadows the package.
- `types.py` shadows stdlib `types`. Use built-in `dict`, `list`, `set` —
  never `Dict`, `List`, `Set` type hints.
- WebFighter dataclass crashed once because fields with defaults were placed
  before fields without. Always read the whole class before adding fields.

## Workflow

Before editing any unfamiliar file:
1. Read it. Don't edit from memory.
2. If it's a dataclass, read the WHOLE class — defaulted fields must come
   AFTER non-defaulted fields.
3. If you're not sure where a function lives, trace the import:
   "what file is loaded when X is imported from Y? read the sys.path setup."

After editing a .py file:
1. Run `python3 -c "import ast; ast.parse(open('FILE').read())"` to syntax-check.
2. Show me a diff. I want to see what changed before declaring done.

I will:
- Restart Flask myself. You don't run servers.
- Test in the browser and report terminal output.
- Save to slot3 (main save).

## Communication

- Direct, concise. Match my energy.
- Diagnosis before code. Tell me the cause before writing the fix.
- Working copy discipline. Always read the file from disk before editing.
- Fix the engine, not the output. No band-aids. Find root cause.
- "Be proactive about crashes" — when I say this, triple-check spacing,
  imports, dataclass field ordering, name shadowing. Predict what will
  break before I find out at runtime.
- I will interrupt if you go the wrong direction. Don't take it personally.

## Do not

- Do not run the Flask server. I run it.
- Do not commit anything. No git operations unless I explicitly ask.
- Do not "improve" code I didn't ask you to touch.
- Do not flatten folder structure or move CLI files.
- Do not use `Dict`/`List`/`Set` type hints (types.py shadows stdlib).
- Do not edit top-level `core/`, `entities/`, `systems/` if the goal is to
  affect the web app — those don't reach it.

## Current top-of-list

1. **DONE 2026-04-27** — Wire FOTN module to UI. Verified in play-test (FOTN headlines rendering on dashboard for Watanabe-Murphy Wk11 and Shavkat-MacDonald Wk13).
2. **DONE 2026-05-02** — Fighter profile UI polish. Five-component bundle shipped at commit `f9c2a07`: style-class banner with family taglines, promoted nickname rendering, 4-quadrant accent-colored stat blocks (Striking=red / Physical=amber / Grappling=blue / Mental=teal), W(KO)-L(KO)-D record format. Verified across striker / grappler / hybrid / champion profiles. Nickname rendering ships dormant — generation/wiring filed at `memory/nickname_population_investigation_2026-05-02.md` for next session.
3. Card builder slot assignment — `calculate_matchup_score` and `assign_slot`
   already exist in `cage_dynasty_web/card_builder.py`, but the bridge
   defaults everything to prelim. Need to wire them.

## Key constants (don't change without telling me)

- `cage_dynasty_web/fight_engine.py`: `DAMAGE_MULTIPLIER = 0.55`
- `cage_dynasty_web/fight_integration.py`: `FI_DAMAGE_MULTIPLIER = 0.32`
- Submission threshold: 70.0
- Rankings: `MAX_MOVE = 3`, `NEW_ENTRY_CAP = 8`
- Contract: `HOLDOUT = 25`, `WALKOUT = 10`, `HOLDOUT_WINDOW = 4 weeks`

## Terminal diagnostics (for tuning)

- 📊 [DFC N] — fight card summary (KO/TKO/SUB/DEC counts)
- 🤕 [INJURY REPORT] — weekly injury load
- 📋 [AI CONTRACT] — expirations
- 🥊 [AI SIGNING] — signings with personality scores
- 📈 [RANKINGS] — top-5 entries
- 😐 [MORALE] — fighter morale below 50


- CLI fork (`core/game_state.py`) has a country/name mismatch bug:
  fighter assigned name from country pool but `country` field re-rolls
  from a 5-element short-code list. Web fork is correct. Do not consolidate
  forks without auditing downstream `country == "..."` comparisons.